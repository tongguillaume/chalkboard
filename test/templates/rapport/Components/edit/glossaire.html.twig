<div class="accordion">
    <div class="accordion-group">
        <div class="accordion-heading area">
            <div class="accordion-heading area">
                <div class="accordion-toggle upperCase">
                    <a id="bloc_glossaire" style="padding-left: 25px;" data-toggle="collapse" href="#glossaire">
                        {{ traduction.glossaire | trans }}
                    </a>
                </div>
            </div>
            <div class="accordion-body collapse hide" id="glossaire">
                <div class="accordion-inner">
                    <div class="accordion" id="equipamento0">
                        <div id="summary">
                            <p class="collapse" style="text-align: justify" id="collapseSummary387">
                                {% for key, glossaire in glossaires %}
                                    <h3> {{ key }} </h3>
                                {% for mot in glossaire %}
                                    <p> {{ mot }}</p>
                                    {% endfor %}
                            {% endfor %}
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>